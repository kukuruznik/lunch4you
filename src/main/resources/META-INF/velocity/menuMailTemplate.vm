Your token is ${token}.

The menu for today is:<br>
#foreach( ${categoryWithArticles} in ${categoriesWithArticles} )
	#set( $category = $categoryWithArticles.entity )
	<h3>${category.name_cz} / ${category.name_en}</h3>
	#foreach( ${article} in ${categoryWithArticles.items} )
		<a href="${shopURL}#view=order&meal=${article.Id}&token=${token}">
		${article.name_cz}
		#if( ${article.name_en} != "" )
			/ ${article.name_en}
		#end 
		</a>
		<br>
	#end

#end

