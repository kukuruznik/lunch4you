Your token is ${token}.

categoriesWithArticles : ${categoriesWithArticles.size()}

The menu for today is:
#foreach( ${categoryWithArticles} in ${categoriesWithArticles} )
	#set( $category = $categoryWithArticles.entity )
	Category: ${category}
	#foreach( ${article} in ${categoryWithArticles.items} )
		Article	- ${article}
	#end

#end


The menu for today is:
#foreach( ${meal} in ${menu} )
	- ${meal.Name_en} / ${meal.Name_cz} for ${meal.Price} bucks (${shopURL}#view=article&meal=${meal.Id}&token=${token})
#end
