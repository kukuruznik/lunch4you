<beans:beans
	xmlns="http://www.springframework.org/schema/security"
	xmlns:beans="http://www.springframework.org/schema/beans"
	xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
	xsi:schemaLocation="http://www.springframework.org/schema/beans 
                    	http://www.springframework.org/schema/beans/spring-beans-3.0.xsd
                    	http://www.springframework.org/schema/security 
                    	http://www.springframework.org/schema/security/spring-security-3.1.xsd">

	<http pattern="/**/*.css" security="none"/>

	<http pattern="/**/*.js" security="none"/>

	<http pattern="/**/*.ejs" security="none"/>

	<http pattern="/**/*.html" security="none"/>

	<http pattern="/**/*.png" security="none"/>

	<http use-expressions="true">
		<intercept-url pattern="/backoffice/login.json" access="permitAll"/>
		<intercept-url pattern="/backoffice/**/*.json" access="isAuthenticated()"/>
		<intercept-url pattern="/**/*.json" access="permitAll"/>
		<form-login login-page="/backoffice/login.json"/>
		<logout logout-success-url="/loggedOut.json"/>
	</http>

	<authentication-manager>
		<authentication-provider>
			<password-encoder hash="plaintext" />
			<user-service>
				<user name="kuchar" password="szakacs" authorities="supervisor" />
			</user-service>
		</authentication-provider>
	</authentication-manager>
</beans:beans>
